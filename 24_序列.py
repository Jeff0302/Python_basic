'''
    序列(sequence)
    - 序列是Python中最基本的數據結構
    - 數據結構是計算機中數據存儲的方式
    - 序列用於保存一組有序的數據，所有的數據在序列當中都有唯一的位置(索引)
        並且序列中的數據會依照添加的順序來分配索引
    - 序列的分類
        可變序列 (序列中的元素可以改變):
            > list(列表)
        不可變序列  (序列中的元素不可以改變):
            > str (字符串)
            > tuple(元組)
'''